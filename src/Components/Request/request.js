import React from 'react';
import API_BASE_URL from '../../../config';

export default class Request extends React.Component{

    render(){
        return(
            <div>
                <a href="">Authorize YNAB</a>    
            </div>
        )
    }
}